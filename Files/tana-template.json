{
  "name": "${Work.name}",
  "date": "${Date}",
  "id": "${sys:nodeId}",
  "url": "${sys:nodeURL}",
  "time": "${sys:currentDate}",
  "context": "${sys:context}",
  "refString": "${Ext Ref|?}",
  "doneTime": "${sys:doneTime}"
}